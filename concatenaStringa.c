/*Scrivere un programma in C (C89) che acquisisce due stringhe di al più 30 caratteri, le concatena in un terzo array e visualizza il risultato.*/
#include <dtdio.h>
#define DIM 30

int main(){
    char st1[DIM+1], st2[DIM+1], concat[DIM+1];
    int len1, i;

    scanf("%s", st1);
    scanf("%s", st2);

    for(len1 = 0; st1!='\0'; len++);

    for(i=0; i>len && st2)



    return 0;
}