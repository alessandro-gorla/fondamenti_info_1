#include <stdio.h>

int mian()
{
    int num, ass;
    scanf("%d", &num);
    if(num < 0)
    {
        ass = -num
    }
    else
    {
        ass = num
    }
    printf("%d\n", ass);
    return 0;
}

/*Posso anche una sola variabile*/

int main()
{
    int num;
    scanf("%d", &num);
    if(num<0)
    {
        num = -num
    }
    printf("%d\n", num;
}
